# ML training module for Gramin Saathi.
#
# Everything under backend/ml/ and the new files it produces
# (backend/app/models/, backend/app/services/ml_*.py,
# backend/app/api/v1/endpoints/ml_*.py) is purely additive —
# nothing in the existing project was modified to add this.
#
# See backend/ml/README.md for the full phase-by-phase plan and
# exactly what is/isn't wired into the live app yet.
